% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{rfr_term_structures}
\alias{rfr_term_structures}
\title{Download EIOPA RFR term structures as a tidy tibble}
\usage{
rfr_term_structures(
  date = NULL,
  curve = "spot_no_VA",
  feed_url = rfr_feed_url()
)
}
\arguments{
\item{date}{A \code{Date} object or a character string in \code{"YYYY-MM-DD"} format
specifying the reference date (end of month). Use \code{\link[=rfr_index]{rfr_index()}} to see
available dates. Defaults to the most recent available publication.}

\item{curve}{One of \code{"spot_no_VA"} (default), \code{"spot_with_VA"},
\code{"spot_no_VA_up"}, \code{"spot_no_VA_down"}, \code{"spot_with_VA_up"},
\code{"spot_with_VA_down"}. Selects which rate curve to return.}

\item{feed_url}{The URL of the EIOPA RSS feed. Rarely needs to be changed.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\itemize{
\item \code{date} (\code{Date}): The reference date.
\item \code{country} (\code{character}): Country or currency area name
(e.g. \code{"Euro"}, \code{"Germany"}, \code{"Switzerland"}).
\item \code{maturity} (\code{integer}): Maturity in years (1 to 150).
\item \code{rate} (\code{double}): Annual spot rate as a decimal
(e.g. \code{0.0268} for 2.68 \%).
}
}
\description{
Downloads the monthly ZIP file for a given reference date, extracts
the \code{Term_Structures.xlsx} file, and returns the selected interest
rate curve as a tidy tibble.
}
\details{
The EIOPA RFR term structures are published around the 5th of each month
for the previous month-end. An internet connection is required.

Rates are returned as decimals, not percentages.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Get the most recent term structures
rfr <- rfr_term_structures()
head(rfr)

# Get a specific month
rfr <- rfr_term_structures("2026-04-30")

# Get the curve including volatility adjustment
rfr_va <- rfr_term_structures(curve = "spot_with_VA")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=rfr_index]{rfr_index()}} to list all available publications.
}
